{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --loader:.js=jsx --format=esm --outdir=app/assets/builds --public-path=/assets",
    "build:dev": "esbuild app/javascript/*.* --bundle --sourcemap --loader:.js=jsx --format=esm --outdir=app/assets/builds --public-path=/assets --watch"
  },
  "dependencies": {
    "esbuild": "^0.23.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
